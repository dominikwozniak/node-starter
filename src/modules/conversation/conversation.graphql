type Conversation {
  id: Int!
  participants: [ConversationUser]
  messages: [ConversationMessage]
  createdAt: DateTime!
  updatedAt: DateTime!
}

# TODO: replace to separate file / module
type ConversationUser {
  userId: Int
  conversationId: Int
}
